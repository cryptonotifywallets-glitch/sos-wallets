{
  "name": "sos-wallets",
  "version": "1.0.0",
  "description": "SOS WALLETS v2 — Advanced Crypto Wallet with backend + admin panel",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install",
    "start": "cd backend && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
